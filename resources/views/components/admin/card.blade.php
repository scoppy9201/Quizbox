@props(['title', 'value'])

<div class="bg-card border border-border rounded-xl shadow-sm p-6">
    <p class="text-sm text-muted-foreground">{{ $title }}</p>
    <h2 class="text-3xl font-bold text-primary mt-2">{{ $value }}</h2>
</div>
